# Agents 多 AI 协作平台 —— 项目技术报告

> 版本：v2.0（微信群聊式架构）
> 日期：2026-05-21
> 作者：Claude Code 协作开发

---

## 一、项目概述

**Agents** 是一个基于 Claude Code SDK 的多 AI 协作平台。用户在前端配置多个 AI Agent（模型、API 密钥、系统提示、工具权限），将它们组织成「群聊」，通过 moderator 调度或 @ 点名机制让多个 AI 围绕同一话题协同工作，所有中间产物实时写入共享黑板（blackboard）。

### 核心定位

- **不是** 单轮问答机器人
- **不是** 简单的 sequential pipeline
- **是** 微信群聊式的多角色协作环境，AI 之间可以 @ 彼此、接力讨论、共享上下文

### 典型使用场景

| 场景 | 群配置 |
|---|---|
| 渗透测试协作 | recon → vuln → exploit → report，moderator 控制阶段推进 |
| 正反辩论 | pro / con 双方辩论，judge 仲裁 |
| 代码评审 | reviewer / critic / writer 三轮 review |
| 红蓝对抗 | red（攻击方）/ blue（防御方），moderator 裁判 |

---

## 二、架构设计

### 2.1 整体架构（三层）

```
┌─────────────────────────────────────────────────────────────┐
│  Frontend (React 18 + Vite)                                  │
│  ├── 💬 ChatView      群聊界面（消息流 + SSE 订阅）            │
│  ├── 🤖 AgentsView    AI 管理（CRUD + emoji + 工具白名单）    │
│  ├── 👥 GroupsView    群管理（成员排序 + moderator 指定）      │
│  └── 📺 Observatory   旧版会话式观察台（兼容保留）             │
├─────────────────────────────────────────────────────────────┤
│  Backend (Fastify + SSE)                                     │
│  ├── /api/agents       AI CRUD                              │
│  ├── /api/groups2      群 CRUD + 成员管理                    │
│  ├── /api/groups2/:id/messages   消息读写                   │
│  ├── /api/groups2/:id/stream     SSE 实时流                 │
│  ├── /api/groups2/:id/blackboard 黑板读取                    │
│  └── /api/chat-approvals/:reqId  工具审批                    │
├─────────────────────────────────────────────────────────────┤
│  Core (@agents/core)                                         │
│  ├── AgentRuntime      Claude SDK 封装（query + tool 回调）   │
│  ├── Blackboard        文件系统黑板（meta/shared/agent区）    │
│  ├── SessionLayout     工作目录结构管理                      │
│  ├── Orchestrator      旧版顺序调度器（兼容保留）              │
│  └── ProviderRegistry  CC-Switch 厂商解析（UUID + name 双回退）│
└─────────────────────────────────────────────────────────────┘
```

### 2.2 数据流（一次用户发言到 AI 回复）

```
用户输入 ──POST──▶ /api/groups2/:id/messages
                    │
                    ▼
              1. 保存消息到 SQLite
              2. SSE broadcast "message" 事件
              3. 解析 @mentions
                    │
                    ▼
              调度器 tickScheduler()
              ┌─────────────────────────┐
              │ queue = [@mentions]     │
              │ while step < 20:        │
              │   if queue: pop next    │
              │   elif moderator:       │
              │     run moderator ──▶   │
              │     parse JSON decision │
              │     "next": "agent-id"  │
              │   else: first member    │
              │   run agent turn ──▶    │
              │   save message          │
              │   parse @ in reply      │
              │   append to queue       │
              └─────────────────────────┘
                    │
                    ▼
              SSE 流推送：
              - moderator-decision
              - agent-thinking
              - message（agent 回复）
```

### 2.3 黑板模式（Blackboard Architecture）

黑板是 append-only 的文件系统目录：

```
workspaces/
└── <group-id>/
    ├── blackboard/
    │   ├── _meta.md         ← 群目标 / 上下文（meta）
    │   ├── _shared.md       ← 公共协作区（shared）
    │   └── agents/
    │       ├── recon.md     ← recon agent 的私有工作区
    │       ├── vuln.md
    │       └── ...
    ├── turns/               ← 旧版回合日志
    └── session.json         ← 旧版会话元数据
```

- **meta**：群级别的 system prompt，所有 agent 都可见
- **shared**：agent 之间共享的发现、结论
- **agent:xxx**：每个 agent 的私有工作区，别人不读（只写自己的）

---

## 三、技术栈

| 层级 | 技术 | 说明 |
|---|---|---|
| 运行时 | Node.js 22+ | `node:sqlite` 内置模块（无需原生依赖） |
| 包管理 | npm workspaces | 4 个子包：core / cli / server / web |
| 类型系统 | TypeScript 5.6 | composite project refs |
| 后端框架 | Fastify 4.x | 轻量、SSE 原生支持 |
| 前端框架 | React 18 + Vite 5 | SPA，tab 路由 |
| 通信 | SSE (EventSource) | 单向 server→client 实时推送 |
| 数据库 | SQLite (`node:sqlite`) | 3 张表：agents / groups / messages |
| AI SDK | @anthropic-ai/claude-agent-sdk | query() + canUseTool 回调 |
| 容器化 | Docker + Docker Compose | 多阶段构建，~60MB runner 镜像 |
| 密钥管理 | CC-Switch | 复用宿主机已配置的厂商（bind mount 只读） |

---

## 四、核心功能详解

### 4.1 AI 管理（AgentsView）

每个 AI 可配置：

| 字段 | 说明 |
|---|---|
| **id** | 英文短名，URL/数据库主键，仅 `[a-zA-Z0-9_-]` |
| **name** | 显示名，可中文 |
| **emoji** | 头像，群聊和列表中展示 |
| **providerId** | CC-Switch 厂商标识（UUID 或 name 如 `ikunn`） |
| **model** | 覆盖默认模型（如 `claude-opus-4-6`） |
| **systemPrompt** | 角色定义、工作约束、@ 规则 |
| **allowedTools** | 白名单（留空 = SDK 默认全开） |
| **disallowedTools** | 黑名单（优先级高于白名单） |
| **permissionMode** | 4 档：`default` / `approve-dangerous` / `auto-allow` / `deny-all` |
| **maxTurns** | 单次上场内部 tool 循环上限（1~N） |

### 4.2 群管理（GroupsView）

| 字段 | 说明 |
|---|---|
| **id** | 群唯一标识（同上规则） |
| **name** | 群显示名 |
| **emoji** | 群头像 |
| **description** | 一句话描述 |
| **moderatorAgentId** | 指定主持人 agent（可选） |
| **meta** | 群级 system prompt，所有 AI 可见 |
| **memberIds** | 成员顺序数组（上下可调） |

**moderator 模式 vs 无 moderator 模式**：

- **有 moderator**：每次发言后 moderator 决策下一个谁来说（JSON: `{"next":"agent-id","reason":"..."}`）
- **无 moderator**：用户用 `@id` 直接点名；没点名时按成员顺序轮转

### 4.3 群聊界面（ChatView）

三栏布局：

- **左侧**：群列表，点击切换
- **中间**：消息流
  - 用户消息：右气泡（蓝色背景）
  - Agent 消息：左气泡 + emoji 头像
  - Moderator 决策：居中虚线胶囊（紫色）
  - 底部状态栏：显示当前调度状态（idle / thinking / busy）
- **右上抽屉**：黑板（meta / shared / agent:xxx 切换）
- **底部输入框**：`Ctrl+Enter` 发送，支持 `@id` 点名

### 4.4 工具审批（Permission System）

当 agent 调用敏感工具（Write/Edit/Bash）时触发审批弹窗：

```
┌──────────────────────────────┐
│ ⚠️ 审批请求 #apv-3           │
│ Agent: recon                 │
│ Tool: Bash                   │
│ Target: /app/workspaces/...  │
│ Reason: 执行 curl 探测       │
│                              │
│ [允许] [拒绝并说明]           │
└──────────────────────────────┘
```

审批通过 → agent 继续执行；拒绝 → agent 收到反馈消息，尝试替代方案。

---

## 五、API 设计

### 5.1 REST API

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/health` | 健康检查 |
| GET | `/api/providers` | 列出 CC-Switch 厂商（密钥脱敏） |
| GET | `/api/agents` | 列出所有 AI |
| POST | `/api/agents` | 创建 AI |
| PUT | `/api/agents/:id` | 更新 AI |
| DELETE | `/api/agents/:id` | 删除 AI |
| GET | `/api/groups2` | 列出所有群 |
| POST | `/api/groups2` | 创建群 |
| PUT | `/api/groups2/:id` | 更新群 |
| DELETE | `/api/groups2/:id` | 删除群（连带聊天记录） |
| POST | `/api/groups2/:id/members` | 批量设置群成员 |
| GET | `/api/groups2/:id/messages` | 获取消息历史 |
| POST | `/api/groups2/:id/messages` | 发送消息（触发调度） |
| POST | `/api/groups2/:id/abort` | 中断当前调度 |
| GET | `/api/groups2/:id/blackboard?label=` | 读取黑板 |
| POST | `/api/chat-approvals/:reqId` | 审批工具请求 |

### 5.2 SSE 事件流

订阅路径：`GET /api/groups2/:id/stream`

| 事件名 | 触发时机 |
|---|---|
| `message` | 新消息（用户/agent/moderator） |
| `moderator-decision` | moderator 选择了下一个发言者 |
| `agent-thinking` | 某 agent 开始思考 |
| `agent-message` | agent 内部 SDK 消息（调 tool 的 intermediate） |
| `scheduler-skip` | 调度器跳过（busy 等原因） |
| `scheduler-warn` | 警告（moderator JSON 解析失败等） |
| `scheduler-error` | 严重错误 |
| `scheduler-idle` | 调度结束，回到空闲 |
| `approval-pending` | 有工具等待审批 |

---

## 六、部署指南

### 6.1 前置条件

- Docker + Docker Compose
- CC-Switch 已配置至少一个厂商（`~/.cc-switch` 存在）
- Node.js 22+（仅本地开发需要）

### 6.2 启动

```bash
cd E:/agents
docker compose up -d
```

访问：`http://localhost:3010`

### 6.3 数据持久化

| 挂载点 | 内容 |
|---|---|
| `./configs` | agent / group / provider JSON 配置 |
| `./workspaces` | SQLite 数据库 + 黑板文件 |
| `~/.cc-switch` | CC-Switch 厂商密钥（只读） |

**首次启动**会自动将 `configs/agents/*.json` 和 `configs/groups/*.json` 迁移到 SQLite，之后 JSON 只作为备份/模板。

### 6.4 重建镜像

```bash
docker compose down
docker build -t agents:latest .
docker compose up -d
```

---

## 七、项目结构

```
agents/
├── package.json                  # 根 package，npm workspaces
├── tsconfig.json                 # TypeScript 复合项目配置
├── Dockerfile                    # 多阶段构建（base → build → runner）
├── docker-compose.yml            # 端口 3010:3000
├── .dockerignore                 # 排除 tsbuildinfo（关键！）
│
├── configs/
│   ├── agents/                   # 12 个预设 agent JSON
│   ├── groups/                   # 4 个预设 group JSON
│   ├── claudemd/                 # 各 agent 的 system prompt 文本
│   └── providers.example.json    # 厂商配置模板
│
├── packages/
│   ├── core/                     # 核心库（Blackboard/Runtime/Registry）
│   │   ├── src/
│   │   │   ├── blackboard/
│   │   │   │   └── blackboard.ts
│   │   │   ├── runtime/
│   │   │   │   ├── agent.ts      # AgentRuntime（SDK 封装）
│   │   │   │   └── approval.ts   # 审批接口定义
│   │   │   ├── providers/
│   │   │   │   └── index.ts      # ProviderRegistry（UUID+name 回退）
│   │   │   └── orchestrator/
│   │   │       └── orchestrator.ts  # 旧版顺序调度器
│   │   └── tsconfig.json
│   │
│   ├── server/                   # Fastify HTTP 服务器
│   │   ├── src/
│   │   │   ├── index.ts          # 入口：注册路由 + 启动
│   │   │   ├── db.ts             # SQLite 层（agents/groups/messages）
│   │   │   ├── routes-agents.ts  # /api/agents CRUD
│   │   │   ├── routes-groups.ts  # /api/groups2 CRUD
│   │   │   └── routes-chat.ts    # 消息/调度/SSE/黑板/审批
│   │   └── tsconfig.json
│   │
│   ├── web/                      # React SPA
│   │   ├── src/
│   │   │   ├── main.tsx          # 渲染 <Shell />
│   │   │   ├── Shell.tsx         # Tab 路由壳
│   │   │   ├── App.tsx           # 旧版 Observatory（兼容）
│   │   │   ├── api.ts            # chatApi + 旧 api
│   │   │   ├── styles.css        # 全部样式
│   │   │   ├── views/
│   │   │   │   ├── ChatView.tsx      # 群聊主界面
│   │   │   │   ├── AgentsView.tsx    # AI CRUD
│   │   │   │   └── GroupsView.tsx    # 群 CRUD
│   │   │   └── components/
│   │   │       └── EmojiPicker.tsx   # emoji 选择器
│   │   ├── index.html
│   │   └── vite.config.ts
│   │
│   └── cli/                      # 命令行入口（旧版兼容）
│       └── src/
│           └── index.ts
│
└── workspaces/                   # 运行时产物（SQLite + 黑板）
    └── agents.db
```

---

## 八、已知问题与改进方向

### 8.1 当前限制

| # | 问题 | 影响 |
|---|---|---|
| 1 | **调度成本翻倍** | 每条消息至少 2 次 LLM 调用（moderator + agent） |
| 2 | **全串行无并行** | 多 AI 不能同时思考，本质还是轮询 |
| 3 | **HISTORY_WINDOW = 30** | 长对话丢上下文，无摘要机制 |
| 4 | **无单测** | routes-chat.ts 调度逻辑裸奔 |
| 5 | **前端 402KB 单 chunk** | 未 code-split，首屏加载偏重 |
| 6 | **CLAUDE.md 未加载** | `settingSources: []` 显式禁用了 SDK 的 CLAUDE.md 自动加载 |
| 7 | **moderator JSON 解析脆弱** | 输出格式不对直接 break 整个循环 |

### 8.2 改进建议（优先级排序）

1. **moderator 降级为可选**
   - 简单场景用规则调度（轮询 + @ 优先）
   - 复杂场景才请 LLM 决策
   - 或改为"agent 自调度"：每个 agent 回复末尾可带 `@next` 或 `#stop`

2. **增加对话摘要机制**
   - 超过 30 条时自动生成 summary，替换为 `Summary + 最近 10 条`

3. **前端 code-split**
   - `React.lazy` 分 chunk，Observatory 作为独立 chunk

4. **补测试**
   - 调度循环的 edge case（空群、moderator 缺失、JSON 解析失败、@ 循环）
   - SQLite CRUD 的边界

5. **并行模式（长期）**
   - 让 recon 和 vuln 同时跑，结果汇总后再下一轮
   - 需要改调度器为 DAG 或 Promise.all 结构

6. **CLAUDE.md 选择性加载**
   - 当前全部禁用是因为每个 agent 的 privateDir 不同，SDK 找不到统一的 `.claude/CLAUDE.md`
   - 可考虑在 `buildSystemPrompt()` 中手动读取并拼接

---

## 九、总结

Agents v2.0 完成了从"会话式顺序执行"到"微信群聊式多角色协作"的架构转型。核心创新在于：

- **moderator 调度**：用 JSON 决策替代硬编码 pipeline，灵活度大幅提升
- **@ 点名机制**：用户可直接干预调度顺序，不完全依赖 moderator
- **SQLite 持久化**：agent/group/message 全进数据库，配置可热修改
- **CC-Switch 复用**：不自己管理密钥，复用宿主机的厂商配置
- **兼容旧 UI**：Observatory 作为 tab 保留，旧 session 数据不丢

当前状态：**功能完整，可运行，架构方向正确**。下一步建议聚焦「降低成本」和「增加测试」两件事。
